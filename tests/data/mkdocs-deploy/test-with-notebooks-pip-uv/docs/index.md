# Test Notebook Documentation

This fixture exercises the `notebooks-path` input on the `mkdocs-deploy`
action. The action should execute `docs/notebook/hello.ipynb` and leave its
outputs populated before the mkdocs build.
